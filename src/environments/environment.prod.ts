/* export const environment = {
  production: true,
  originURL: process.env['originURL'],
  apiUrl: process.env['apiUrl'],
  WSapiUrl: process.env['WSapiUrl'],
  featureToggle: false,
  privateKeyPath: process.env['privateKeyPath'],
  certificatePath: process.env['certificatePath'],
}; */
